using UnityEngine;
using System.Collections;

/**
 * This class is used to insert all functions that are used by all enemies regarding their behaviour, 
 * this way all enemies can inherit this classes functions and we only have to write them once 
 */
public class Enemy : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
